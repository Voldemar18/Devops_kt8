🎯 Обзор проекта
Universal DevSecOps Reporter - это универсальный скрипт для автоматического сбора и анализа отчетов проверок безопасности и качества кода. Проект был создан для автоматизации процесса мониторинга безопасности в DevOps-процессах.

Что я сделал:
✅ Создал систему парсинга отчетов из популярных инструментов безопасности

✅ Реализовал генерацию единого HTML-отчета

✅ Настроил систему уведомлений через разные каналы

✅ Сделал модульную архитектуру для легкого расширения

✅ Подготовил интеграцию с CI/CD системами

🚀 Возможности
📊 Универсальный парсинг: Поддержка JSON, XML, TXT форматов

🎨 Визуальные отчеты: Генерация HTML с фильтрацией и сортировкой

🔔 Умные уведомления: Email, Slack, Telegram оповещения

🔌 Расширяемость: Легко добавить поддержку новых инструментов

⚡ Автоматизация: Готовая интеграция с CI/CD пайплайнами

Поддерживаемые инструменты:
SAST: SonarQube, Bandit

DAST: OWASP ZAP

Linters: Flake8

SCA: (можно добавить Trivy, Safety)

Test Coverage: JaCoCo (в разработке)

📁 Структура проекта
universal_devsecops_reporter/
│
├── 📂 parsers/                 # Модули парсинга отчетов
│   ├── __init__.py
│   ├── base_parser.py         # Базовый класс всех парсеров
│   ├── sonarqube_parser.py    # Парсер SonarQube
│   ├── zap_parser.py          # Парсер OWASP ZAP
│   └── bandit_parser.py       # Парсер Bandit
│
├── 📂 utils/                  # Вспомогательные модули
│   ├── __init__.py
│   ├── html_generator.py      # Генератор HTML отчетов
│   └── notifier.py           # Отправка уведомлений
│
├── 📂 templates/              # HTML шаблоны
│   └── report_template.html   # Шаблон отчета
│
├── 📂 config/                 # Конфигурационные файлы
│   ├── tool_config.yaml      # Настройки инструментов
│   └── notification_config.yaml # Настройки уведомлений
│
├── 📂 test-reports/           # Примеры отчетов для тестирования
│   ├── bandit-report.json
│   ├── sonarqube-report.json
│   └── zap-report.json
│
├── 📜 universal_reporter.py   # Главный исполняемый скрипт
├── 📜 requirements.txt        # Зависимости Python
└── 📜 README.md              # Эта документация

1. Клонирование и настройка
# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate
2. Установка зависимостей
bash
pip install PyYAML requests jinja2 python-dotenv
3. Быстрый старт
bash
# Запуск с тестовыми отчетами
python universal_reporter.py --reports-dir ./test-reports --output-dir ./reports

# Откройте сгенерированный отчет
start reports/comprehensive_devsecops_report.html
🎮 Использование
Базовые команды
bash
# Стандартный запуск
python universal_reporter.py --reports-dir ./security-scans --output-dir ./reports

# С уведомлениями
python universal_reporter.py --reports-dir ./scans --output-dir ./reports --send-notifications

# С кастомными конфигами
python universal_reporter.py --config ./my-config.yaml --notify-config ./my-notifications.yaml
